format_version: "11"
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  test_integration:
    steps:
    - script:
        title: Run integration tests
        inputs:
        - content: go test -tags=integrationtests ./integrationtests -v
